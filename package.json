{
  "name": "agents-pixel-film",
  "version": "1.0.0",
  "private": true,
  "description": "«Агенты» — 45-second pixel-art film for Pixel by Pixel (@p_by_p). Every frame and every sound is computed by code.",
  "type": "module",
  "scripts": {
    "build": "node tools/build.js",
    "audio": "node src/audio/render.js",
    "frame": "node tools/frame.js",
    "sheet": "node tools/contact.js",
    "qr": "uv run --with opencv-python --with numpy python tools/qr_check.py out/film.mp4"
  },
  "engines": {
    "node": ">=20"
  }
}
